services:
  - type: web
    name: informa-clima
    env: python
    buildCommand: |
      apt-get update && apt-get install -y gcc g++ libatlas-base-dev
      pip install -r requirements.txt
    startCommand: "uvicorn src.main:app --host 0.0.0.0 --port $PORT"
    pythonVersion: 3.11.3
